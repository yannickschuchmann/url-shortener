<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Murl.ml | Short your URL" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <div class="container">
        <%= render "layouts/header" %>
        <% if notice %>
        <div class="row">
            <div data-alert class="alert-box success column small-12">
                <%= notice %>
                <a href="#" class="close">&times;</a>
            </div>
        </div>
        <% end %>
        <% if alert %>
            <div class="row">
                <div data-alert class="alert-box warning column small-12">
                    <%= alert %>
                    <a href="#" class="close">&times;</a>
                </div>
            </div>
        <% end %>
        <%= yield %>
    </div>
    <%= javascript_include_tag "application" %>
  </body>
</html>